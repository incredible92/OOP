console.log('cv class')

const rectangle = {
    width: 10,
    heigh: 20,
    location: { x:0, y: 1 },
    draw() {
        console.log(`drawing...spec => width: ${this.width} height:${this.height} `)
    }
}
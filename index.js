console.log('codeVillage class')

const rectangle = {
    width: 50,
    heigh: 30,
    location: { x:0, y: 1 },
    draw() {
        console.log(`drawing...spec => width: ${this.width} height:${this.height} `)
    }
}